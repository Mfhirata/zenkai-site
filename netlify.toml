

# Configurações básicas do build (mantém ou adiciona se não tiver)
[build]
  command = "npm run build"
  publish = "dist"

# Plugin Visual Editor - usa a versão latest automaticamente (sem "version")
[[plugins]]
  package = "@netlify/plugin-visual-editor"